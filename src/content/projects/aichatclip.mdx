---
title: AIChatClip
meta:
  date: 2026-03-27
  duration: 2026.01〜現在 (Ongoing)
  link: https://aichatclip.com
attributes:
  roles:
    - ui-design
    - frontend-dev
    - backend-dev
  stack:
    - React Router v7
    - Hono
    - Cloudflare Workers
    - Cloudflare D1
    - Durable Objects
    - Workers AI
    - WXT
    - Obsidian API
    - Tailwind CSS
    - Turborepo
---

## TL;DR

ChatGPT / Claude / Gemini / Grok の回答を、ワンクリックで Obsidian / Notion / Webhook に投げ込む個人開発の SaaS です。企画から実装、ストア審査、運用、マーケまで一人でやっています。

| 項目 | 内容 |
|---|---|
| Role | 企画 / 設計 / UI・ロゴ / 全実装 / 審査対応 / プロモ動画 / マーケ |
| Status | Chrome / Firefox ストア公開済み / 課金実装済み / 有料ユーザー獲得済み |
| Stack | Cloudflare Workers / D1 / Durable Objects / Workers AI / Better Auth / WXT / Obsidian Plugin |
| URL | https://aichatclip.com |

> **Don't Archive, Just Clip.** — ログを墓場にするな、知識を切り抜け。

## なんで作ったか

AI チャットで「これ良い回答だな」と思っても、放っておくと履歴の海に沈んでいく感じがずっと気持ち悪くて。Obsidian や Notion をメインの知識ベースにしているなら、そっちに流し込めればいいだけのはずなのに、その「最後の 1cm」が地味に面倒くさい、という個人的な不満が起点でした。

最初は **n8n で自分用に組んでいたパイプライン** で、サーバー内で完結してローカルフォルダに MD ファイルを直書きするだけのシンプルな構成でした。Obsidian プラグインすら噛んでいない、本当に自分用のやつ。

これを友人に見せたときに反応が良くて、「n8n で組むと半日かかるから、月 1,000 円で使える形にするならやる?」って聞いたら **即決で「やる」** と返ってきました。1,000 円で迷わず即決される需要があるなら、これは出していいやつだなと判断して着手しました。

Cloudflare Workers / Workers AI / D1 で組めば、変動費はほぼ 0 円・固定費は最初の開発工数だけ、と試算が立っていたのも大きかったです。

開発は AI による実装速度を活かして、2026 年 1 月の着手から **約 2〜3 ヶ月でストア公開** まで持っていきました。

## どう作ったか

Cloudflare の上に、Web / API / 拡張機能 / Obsidian Plugin が同居する monorepo として組んでいます。一人で長く運用したいので、運用負荷を最小にすることを優先しました。

```
┌─────────────────────────────────────────────────────────────┐
│  Browser Extension (WXT + React, MV3/MV2)                   │
│   ↓ Clip 操作                                                │
├─────────────────────────────────────────────────────────────┤
│  Hono on Cloudflare Workers                                 │
│   ├─ Better Auth (Google OAuth)                             │
│   ├─ Workers AI                                             │
│   │   ├─ Qwen3-30B → タイトル・タグ生成                       │
│   │   └─ Qwen Embedding → 保存先フォルダパスの自動判定         │
│   ├─ D1 (Drizzle ORM) → クリップ保存                         │
│   └─ Durable Object → WebSocket Push (Hibernation API)      │
├─────────────────────────────────────────────────────────────┤
│  Obsidian Plugin (TypeScript + esbuild)                     │
│   ↑ WebSocket pull + push                                   │
└─────────────────────────────────────────────────────────────┘
```

| コンポーネント | 技術 | 役割 |
|---|---|---|
| Web | React Router v7 (SSR on CF Workers) + Tailwind v4 + Motion | LP / 設定画面 / 課金ページ |
| API | Hono on Cloudflare Workers | REST / WebSocket / 認証 |
| DB | Cloudflare D1 (SQLite) + Drizzle ORM | クリップ / ユーザー / 課金状態 |
| AI | Workers AI (Qwen3-30B + Qwen Embedding) | タイトル要約 / 自動タグ / 保存先パスの自動判定 |
| Realtime | Durable Objects + WebSocket (Hibernation API) | Obsidian への Push 同期 |
| 認証 | Better Auth (Google OAuth) | 全クライアント横断の SSO |
| Extension | WXT + React (Chrome MV3 / Firefox MV2) | クリップ取得 / UI |
| Obsidian | TypeScript + esbuild | プラグイン / ローカル書き込み |
| Monorepo | Turborepo + pnpm workspaces | 5 パッケージを一元管理 |

## 設計でちゃんと考えたところ

技術選定だけは自分でやって、AI には実装の手数を増やすところを任せる、というスタンスで作っています。なんで選んだかを後から一応ちゃんと喋れる方が、運用で困らないので。

### Durable Objects + WebSocket

複数 Obsidian インスタンスがつながる前提だと、「優先度の高いデバイスにだけ通知したい」という要求が出てきて、サーバー側に状態を持たないと無理でした。Durable Objects は単一書き込み点を保証してくれて、Hibernation API で常時接続のコストを抑えられるので、月額コストの話とも整合します。ping でコネクション維持 + 指数バックオフで自動再接続にして、生活のワークフローに溶け込む信頼性に振っています。

### Better Auth

一人運用なので自前認証は維持コストがしんどい。Auth.js / Clerk / Supabase Auth と比べて、Cloudflare Workers + D1 に素直に乗ること、料金が発生しないこと、Google OAuth が即日通ることで Better Auth に決めました。

### Workers AI

タイトル要約とタグ自動生成のために OpenAI / Anthropic を呼ぶと、ユーザー数に比例してコストが線形に伸びるのが嫌で。Workers AI は従量課金が極端に安いし、エッジで完結するのでレイテンシも低い。Qwen3-30B を採用しています。

初期段階でいきなりフロンティアモデルを使うより、中堅性能のモデルで戦った方がいい、というのが今回作って強く感じたところでした。フロンティアモデルは賢く汲み取りすぎてくれるので、こちらのプロンプト精度が伸びにくい。中堅モデルだとプロンプト調整が必須になるので、その筋肉がついてからフロンティアモデルに行く方が結果的にうまく使えると思います。

### Qwen Embedding (保存先パスの自動判定)

クリップを Obsidian のどのフォルダに置くかを自動判定するのに、埋め込みモデルを使っています。最初は LLM に「適切なフォルダを選んで」と聞けばいいかと考えていたんですが、毎回返答がブレるのが運用上きつかった。

埋め込みモデルは同じ入力に対して同じベクトルが返るので、扱いが「ある種決定論的」になる。これが個人サービスでめちゃくちゃありがたくて、AI を組み込むサービスを作るなら「まず埋め込みで解けないか」を最初に検討すべきだなというのが今回の学びでした。普通に AI を使ってるだけだと埋め込みモデルって出てこない選択肢なので、自分でサービスを組んで初めて見えた選択肢でもあります。

### WXT

Chrome MV3 / Firefox MV2 を 1 コードベースで両対応する必要があって、生の WebExtensions API より型安全で HMR が効いて、ストア提出ビルドまで自動化されるので採用。

### Turborepo

web / api / extension / obsidian / shared / db を、型を共有しながら並行で触りたいので入れています。`shared` パッケージで型 / スキーマ / API クライアントを共通化して、サーフェス間で齟齬が出ない構造にしました。

## いまの状況

- **Chrome Web Store**: 公開済み
- **Firefox Add-ons**: 公開済み
- **Obsidian Plugin**: 自動更新対応版を配布中
- **Web**: https://aichatclip.com

### 数字 (2026.05 半ば時点)

- 登録ユーザー: 51 人
  - 3 月末ローンチで、3 月は本当に 1〜2 人ペース
  - 4 月半ばから一気に増え始め、5 月入ってからは週 10 人強 / 月 20 人前後
- クリップ数: 週 41 クリップぐらい
- Cloudflare 月額: ほぼ 0 円 (ドメインの年額月割り分だけ)
  - 直近 30 日の Workers リクエスト数は 26,000 回ほどだが、ほぼ攻撃。まともなリクエストはこのうちごく一部
- 有料ユーザー: 1 名 (もともと「課金するよ」と言ってくれていた友人)
  - 見ず知らずの人から課金が来た瞬間が、たぶん次のお祭り
- ユーザーテストを 2 回回してオンボーディングを改善
- プロモ / ガイド動画は Remotion で制作

### 運用してから直したやつ

- **拡張機能だけ整備したら「完了」表示が出る問題**: 設定が必要な場所は実は 2 箇所 (拡張機能 + Obsidian プラグイン) なのに、拡張機能側だけで完了アイコンが出てしまっていた。Obsidian 側もクリアしないと「完了」にならない流れに直したら、登録だけして使わずに離脱するユーザーが目に見えて減った。ユーザーに正しい状態をフィードバックするだけでこんなに変わるのか、という体感
- **ChatGPT の DOM 変更で拡張機能のアイコンが変な場所に出るようになった**: 「一番最初に拾ったコピーアイコンの隣に配置」というロジックを組んでいたら、ChatGPT 側の構造変更で想定外のアイコンを掴んでしまった。一番最後にまとまって出てくるツールアイコン一覧のところに追加する形に変更
- **Firefox の審査でスパム判定を一度くらった**: 解除メールを送って対応してもらったが、その後新バージョンの審査が 1 ヶ月待ち。Chrome よりだいぶしんどい

### 直近の課題

- Free → Pro への誘導設計（まだマーケに着手していない状態）
- SEO / コンテンツでの流入経路の構築
- オンボーディングのもう一段の磨き込み

### やってみてわかったこと

- Cloudflare スタックをプロダクションで回すとどんな感じか、の土地勘ができた
- 個人サービスで AI を組み込むなら、まず埋め込みモデルで解けないかを検討するのが圧倒的に良い。LLM のブレが運用負荷になる場面が結構ある
- 拡張機能 / Web / API / Obsidian / リアルタイム同期を横断する設計は、モノレポで型を共有しないと結構しんどい
- 1,000 円の課金意思を先に確認してから着手する動き方は、Swept での仮説検証の習慣がそのまま効いた。N=1 の判断だが、これがあったから「売れないかも」と悩み続けずに公開まで一気に走れた

> Web / API のコードは商用運用中なので非公開、拡張機能と Obsidian プラグインは公開しています。設計判断はだいたいこのページに書いた通り。
